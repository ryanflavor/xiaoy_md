# Operations console local development defaults
# Copy to .env.local and adjust tokens for your environment.
# NOTE: The frontend automatically appends "/api" to this base URL.
# Make sure the host/port here is also present in OPS_API_CORS_ORIGINS.
VITE_OPS_API_BASE_URL=http://localhost:9180  # replace with LAN IP (e.g. http://192.168.10.206:9180) when accessing from other devices
VITE_OPS_API_TOKEN=local-dev-ops-token
# Optional override for Vite dev proxy target (defaults to localhost:9180)
# VITE_OPS_API_PROXY_TARGET=http://localhost:9180
